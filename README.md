**deepseek api调用**

本仓库为初次尝试deepseek api调用，后续会继续完善。

main.py中的line 35的history是一个列表，记录了与模型的对话历史。每次调用ask方法时，都会将用户输入和模型回复添加到这个列表中，以便模型能够根据上下文进行回答。同时模型的初始设置在Line35完成，后续可能单独设置一个文件来存储模型的初始设置，以便更灵活地调整和管理。

此时的history存储在内存中，因此每次代码运行都是新的对话，后续可能考虑将history存储在文件或数据库中，以便在不同的会话之间保持对话历史，提供更连续的用户体验。

后续可能考虑将main.py中的代码分散到别的代码文件中，使代码结构更清晰，便于维护和扩展。

当前代码需要code打开deepseek_api文件夹运行，否则无法正确获取到key
